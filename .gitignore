# Environment & secrets
.env
*.env.local

# Database
*.db
*.db-shm
*.db-wal

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
venv/
.venv/
*.egg-info/
dist/
build/
.pytest_cache/
.pytest_tmp/

# Old domain scrape folders
ahrefs.com/
bancatransilvania.ro/
bestetic.ro/
ing.ro/
stancosmin.com/
tmp/

# Lead worker (separate project)
lead_worker/

# Dev tools & junk
cloudflared.exe
cloudflare_tunnel.log
nul
=1.13.0
.wget-hsts
.npmrc.bak
*.zip
*.log

# Merge notes & old changelogs (optional — remove these lines to keep them)
merge_notes/
prompts_backup/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Claude Code local settings (keep .claude/haiku_task_guide.md if you want)
.claude/settings.local.json
.claude/launch.json

# Screaming Frog MCP server URL is machine-local (localhost, this machine's
# SF install and licence) -- not portable to anyone else, so not committed.
# The port itself is stable: mcpserver.port in ~/.ScreamingFrogSEOSpider/
# spider.config, currently 11435.
.mcp.json

# Debug scripts
debug_*.py
plan2.md

# Consolidation-session working files (Etapa 5.6, docs/CONSOLIDATION_PLAN.md)
BUILD_LOG.md
*_report.txt
website_llm_analyzer.log.*
uvicorn_*.txt

# Production database directory: the live DB plus cleanup backups/dumps.
# *.db already covers the databases; this also catches deleted_orphans.json,
# which scripts/cleanup_orphans.py writes next to the DB it cleans.
api/data/
